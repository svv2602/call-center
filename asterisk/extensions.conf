; Call Center AI — Asterisk Dialplan
; AudioSocket: 16kHz, 16-bit signed linear PCM

[incoming]
; All incoming calls → AI agent via AudioSocket
exten => _X.,1,NoOp(AI Call Center: incoming from ${CALLERID(num)})
 same => n,Answer()
 same => n,Set(CHANNEL(audioreadformat)=slin16)
 same => n,Set(CHANNEL(audiowriteformat)=slin16)
 same => n,AudioSocket(${UNIQUE_ID},127.0.0.1:9092)
 same => n,Hangup()

[transfer-to-operator]
; Transfer to operator queue (called via ARI redirect)
exten => _X.,1,NoOp(Transfer to operator: ${CALLERID(num)})
 same => n,Queue(operators,t,,,120)
 same => n,VoiceMail(operator@default)
 same => n,Hangup()
