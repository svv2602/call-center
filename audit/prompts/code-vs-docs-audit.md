# Промпт: Аудит соответствия кода документации

## Контекст

Ты — независимый аудитор проекта Call Center AI. Проект прошёл 4 фазы разработки. Твоя задача — провести всестороннюю проверку соответствия реализованного кода проектной документации из папки `/doc`.

## Инструкции

### Шаг 1. Прочитай документацию (обязательно)

Прочитай следующие файлы в указанном порядке:

**Обзор и архитектура:**
1. `doc/README.md` — навигация, структура проекта
2. `doc/development/00-overview.md` — обзор, канонический список tools, стратегия тестирования, CI/CD
3. `doc/technical/architecture.md` — архитектура (SAD), компоненты, протоколы

**Спецификации по фазам:**
4. `doc/development/phase-1-mvp.md` — MVP: поиск шин, наличие, оператор
5. `doc/development/phase-2-orders.md` — Заказы: статус, создание, подтверждение
6. `doc/development/phase-3-services.md` — Сервисы: шиномонтаж, RAG
7. `doc/development/phase-4-analytics.md` — Аналитика: дашборды, качество, A/B, оптимизация

**Техническая спецификация:**
8. `doc/technical/data-model.md` — ERD, таблицы, индексы, партиционирование
9. `doc/technical/nfr.md` — нефункциональные требования, латентность, алерты
10. `doc/technical/sequence-diagrams.md` — диаграммы последовательности (7 сценариев)
11. `doc/technical/deployment.md` — инфраструктура, Docker Compose
12. `doc/development/api-specification.md` — контракт Store API (все endpoints)

**Безопасность:**
13. `doc/security/threat-model.md` — STRIDE модель угроз
14. `doc/security/data-policy.md` — политика обработки данных, PII
15. `doc/security/risk-matrix.md` — матрица рисков

**Предыдущие аудиты:**
16. `audit/audit-report.md` — первый аудит
17. `audit/doc-audit-2026-02-13.md` — аудит документации
18. `audit/improvement-suggestions.md` — предложения по улучшению

### Шаг 2. Исследуй кодовую базу

Изучи реализацию, проверяя каждый аспект:

```
src/
├── core/           — AudioSocket, CallSession, Pipeline, ARI
├── agent/          — LLM Agent, Tools, Prompts, PromptManager, ABTesting, ModelRouter
├── stt/            — STT engines (Google, Whisper)
├── tts/            — TTS engine (Google) с кэшированием
├── store_client/   — HTTP клиент Store API
├── knowledge/      — RAG: embeddings, vector search
├── logging/        — Структурированный логгер, PII sanitizer
├── monitoring/     — Prometheus метрики, cost tracker
├── tasks/          — Celery: quality evaluator, daily stats, prompt optimizer
├── api/            — FastAPI routes: analytics, auth, prompts, knowledge
├── config.py       — Pydantic Settings
└── main.py         — Entry point

migrations/versions/ — Alembic миграции (001-006)
tests/               — Unit и integration тесты
grafana/             — Dashboards JSON, provisioning
prometheus/          — Alerts, scrape config
alertmanager/        — Telegram webhook config
admin-ui/            — SPA для администратора
docker-compose.yml   — Все сервисы
```

### Шаг 3. Проведи проверку по категориям

Для каждой категории создай таблицу с колонками:
- **Требование** (что написано в документации)
- **Статус** (✅ реализовано / ⚠️ частично / ❌ не реализовано / 🔄 отличается)
- **Файл** (где реализовано)
- **Комментарий** (детали расхождения, если есть)

---

#### 3.1 Канонический список tools

Проверь **точное** соответствие имён и параметров tools из `doc/development/00-overview.md` (секция 3.8) с реализацией в `src/agent/tools.py`.

Каноничный список:
```
search_tires, check_availability, transfer_to_operator,
get_order_status, create_order_draft, update_order_delivery,
confirm_order, get_fitting_stations, get_fitting_slots,
book_fitting, search_knowledge_base
```

Проверь:
- Все tools определены в `src/agent/tools.py`?
- Имена точно совпадают (особенно `create_order_draft`, не `create_order`)?
- Input schema каждого tool соответствует документации?
- Реализация в `src/store_client/client.py` покрывает все tools?

#### 3.2 Архитектура и компоненты

Сравни `doc/technical/architecture.md` с реализацией:
- AudioSocket протокол (типы пакетов: 0x00, 0x01, 0x10, 0xFF)
- Pipeline: AudioSocket → STT → LLM → TTS → AudioSocket
- Barge-in поддержка
- Session state в Redis (TTL 1800s)
- Circuit breaker (aiobreaker, fail_max=5, timeout=30s)
- Структурированный JSON logging с call_id + request_id

#### 3.3 Модель данных

Сравни `doc/technical/data-model.md` с `migrations/versions/001-006`:
- Все таблицы созданы? (customers, calls, call_turns, call_tool_calls, orders, order_items, fitting_stations, fitting_bookings, knowledge_articles, knowledge_embeddings, daily_stats, prompt_versions, prompt_ab_tests)
- Все колонки совпадают с ERD?
- Monthly partitioning для calls, call_turns, call_tool_calls?
- Индексы созданы?
- pgvector для knowledge_embeddings?

#### 3.4 Store API контракт

Сравни `doc/development/api-specification.md` с `src/store_client/client.py`:
- Все endpoints реализованы?
- HTTP методы совпадают (GET/POST/PATCH/DELETE)?
- Idempotency-Key для мутаций (POST /orders, POST /orders/{id}/confirm)?
- Retry логика для 429/503?
- Circuit breaker интеграция?

#### 3.5 NFR (нефункциональные требования)

Сравни `doc/technical/nfr.md` с реализацией:
- Latency budget: STT ≤500ms, LLM ≤1000ms, TTS ≤400ms, Total ≤2000ms
- Prometheus метрики для каждого компонента?
- Алерты: STT >700ms, LLM >1500ms, TTS >600ms, Total >2500ms?
- Session TTL 1800s?
- Горизонтальное масштабирование (stateless Call Processor)?

#### 3.6 Безопасность

Сравни `doc/security/threat-model.md` и `doc/security/data-policy.md` с реализацией:
- PII sanitizer реализован?
- Аудио НЕ записывается (только streaming STT)?
- Транскрипции хранятся 90 дней?
- Bot объявляет автоматическую обработку в начале звонка?
- Prompt injection защита (валидация tool parameters)?
- AudioSocket через VPN/WireGuard (конфигурация)?

#### 3.7 Аналитика (Phase 4)

Сравни `doc/development/phase-4-analytics.md` с реализацией:
- Prometheus метрики (все из секции 4.1)?
- Grafana дашборды (realtime + analytics)?
- Quality evaluation (8 критериев, Claude Haiku)?
- A/B тестирование промптов?
- Алерты (все из списка)?
- Админ-интерфейс (журнал звонков, детали, промпты, KB)?
- Cost optimization (Whisper STT, model routing, TTS cache)?

#### 3.8 Sequence Diagrams

Сравни `doc/technical/sequence-diagrams.md` — проверь, что все 7 сценариев покрыты кодом:
1. Поиск шин
2. Проверка наличия
3. Переключение на оператора
4. Статус заказа
5. Оформление заказа (draft → delivery → confirm)
6. Запись на шиномонтаж
7. Консультация (RAG)

#### 3.9 Deployment и Docker

Сравни `doc/technical/deployment.md` с `docker-compose.yml`:
- Все сервисы присутствуют?
- Порты совпадают?
- Healthcheck'и настроены?
- Volumes для данных?

#### 3.10 Тестирование

Сравни стратегию тестирования из `doc/development/00-overview.md` (секция 5) с `tests/`:
- Unit тесты: покрытие ключевых модулей?
- Integration тесты: pipeline, PostgreSQL, Redis?
- Adversarial тесты: prompt injection, невалидные параметры?
- Load тесты: определены, но могут быть не реализованы?

### Шаг 4. Сформируй отчёт

Итоговый отчёт должен содержать:

1. **Executive Summary** — общая оценка соответствия (процент), критические расхождения
2. **Детальные таблицы** по каждой категории (3.1–3.10)
3. **Критические расхождения** — то, что может вызвать баги или нарушить контракт
4. **Некритичные расхождения** — мелкие отличия, улучшения, TODO
5. **Отсутствующие компоненты** — что описано в документации, но не реализовано
6. **Избыточные компоненты** — что реализовано, но не описано в документации
7. **Рекомендации** — конкретные действия для устранения расхождений

### Формат отчёта

Сохрани отчёт в файл `audit/code-vs-docs-audit-YYYY-MM-DD.md`, где YYYY-MM-DD — текущая дата.

Используй русский язык (как и документация).

## Ограничения

- НЕ вноси изменения в код или документацию — только анализ
- НЕ пропускай файлы — проверяй каждый аспект
- Если файл слишком большой — читай по частям
- Будь объективным — фиксируй и положительные аспекты, и проблемы
- Приоритизируй: критические расхождения → некритичные → улучшения
